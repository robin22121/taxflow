# TaxFlow AI — 실행 계획

> 세무 업무 AI 자동화 플랫폼(가칭 TaxFlow AI / 세무톡) 사업기획서 v2.0의 **워크플로우 설계 / 단계별 로드맵 / MVP 설계 / 마일스톤 / 액션 아이템**을 정리한 문서.
> 시장·기술·리스크 분석 근거는 `research.md` 참고.

---

## 1. 핵심 자동화 로직

### 1.1 원천세 신고 4단계와 우리의 자동화 범위

```
[전체 프로세스]
고객 원시데이터 → 신고서 양식 변환 → 위하고T 입력 → 홈택스 전자신고 → 접수증·납부서 회수
       ▲                  ▲                ▲              ▲                  ▲
       │                  │                │              │                  │
   AI 자동수집         AI 표준화      엑셀 일괄업로드   위하고T 내장기능       자동 회수·전달
   (Phase 1)        (Phase 1)         (Phase 2)        (Phase 2)          (Phase 3)
```

### 1.2 통합 워크플로우

```
[1] 세무사가 대시보드에서 "이번 달 원천세 자료 요청" 버튼 클릭
       ↓
[2] 시스템이 등록된 거래처 100곳에 알림톡 일괄 발송
    (각 거래처별 토큰 URL 포함)
       ↓
[3] 거래처 응답 (3가지 경로)
    ├─ 경로 A: URL 클릭 → 보안 폼에 입력
    ├─ 경로 B: 카톡으로 답장 ("김연호 100, 조명신 120") → AI 파싱
    └─ 경로 C: 엑셀/사진 업로드 → AI 파싱
       ↓
[4] AI가 데이터 정형화 + 직원 매칭
    - 이름·금액 추출
    - 기존 직원 DB와 매칭 (전월 데이터 기반)
    - 신규 의심자 → 후속 질문 자동 발송
    - "저번달과 똑같아요" 같은 상대 표현 처리
       ↓
[5] 세무사 대시보드에서 검증·승인
    - 거래처별 상태 표시 (수신완료/미수신/검증필요)
    - 이상치 자동 하이라이트 (전월 대비 급증 등)
       ↓
[6] 위하고T 표준 엑셀 자동 생성
    - 인적사항·지급액·소득구분코드·세액 자동 계산
    - 위하고T [원천세 신고 → 일괄등록] 양식 100% 호환
       ↓
[7] 세무사가 위하고T에 업로드 → 검증 → 홈택스 전자신고
    (Phase 2에서는 이 부분도 반자동화)
       ↓
[8] 접수증·납부서 자동 회수 → 거래처 자동 전달
    (Phase 3)
```

---

## 2. 단계별 로드맵

### Phase 1: 고객 정보 수집·표준화 (MVP, 0~6개월)

**목표**: 다양한 채널로 들어오는 비정형 데이터를 위하고T 일괄 업로드용 표준 엑셀로 변환

**핵심 기능**
- **다채널 자료 요청 발송** (알림톡 일괄 발송)
- **카톡 메시지 파싱 (AI)**
  - 예: `"김연호 100만원, 박민수 150만원"` → 구조화 JSON
  - `"저번달이랑 똑같아요"` → 전월 데이터 자동 적용
- **음성 통화 자동 텍스트 변환** + AI 파싱
- **엑셀 자동 인식** (컬럼명 다양성 자동 매핑)
- **개별 보안 입력 URL** (주민번호 등 민감정보용)
- **AI 직원 매칭 엔진**
  - 기존 직원 DB와 자동 매칭
  - 신규/퇴사 의심자 자동 분류
  - 후속 질문 자동 발송 (예: "박민수님 신규입사자인가요? 주민번호 입력 부탁드립니다")
- **데이터 검증 대시보드** (세무사 최종 승인)
- **위하고T 일괄등록용 표준 엑셀 자동 생성**

**산출물**: 세무사가 다운로드해서 위하고T에 그대로 업로드 가능한 표준 엑셀

**Phase 1 끝나면 무엇이 가능한가?**
- 고객 응대 시간 80% 절감
- 위하고T 입력은 세무사가 엑셀 업로드 한 번으로 끝
- 신고·납부서까지는 위하고T 기존 워크플로우 활용

---

### Phase 2: 위하고T 업로드·신고 반자동화 (6~12개월)

**목표**: Phase 1 산출물을 위하고T에 업로드하고 신고까지 이어주는 흐름 자동화

**핵심 기능**
- **위하고T 엑셀 자동 업로드 어시스턴트**
  - 세무사 PC에 경량 에이전트 설치 (브라우저 확장 또는 데스크톱 앱)
  - 위하고T [일괄등록] 화면에서 "TaxFlow에서 가져오기" 버튼 클릭
  - 표준 엑셀 자동 다운로드·업로드·검증
- **신고 전 자동 검증**
  - 더존 위하고T 양식 규격 검증
  - 누락 필드·계산 오류 사전 감지
  - 전월 대비 이상 변동 알림
- **홈택스 전자신고 트리거**
  - 위하고T 내장 신고 기능 활용 (공동인증서는 세무사 PC 로컬에서)
  - 신고 진행 상태 실시간 추적
- **거래처 직원 마스터 동기화**
  - 위하고T에서 거래처 직원 명단 주기적 가져오기
  - 신규 직원 매칭 정확도 향상

**왜 "에이전트 방식"인가?**
- 공동인증서는 클라우드 서버에서 사용 불가 (보안·법적 이슈)
- 위하고T 로그인은 세무사 PC에서 진행해야 자연스러움
- 블랙피그AI도 동일한 이유로 로컬 PC 에이전트 방식 채택

---

### Phase 3: 신고 후 자동화 + 입·퇴사 처리 (12~18개월)

**핵심 기능**
- **접수증·납부서 자동 회수**
  - 거래처별 자동 전달 (알림톡·이메일)
- **신규 입사자 등록 흐름**
  - AI 후속 질문 → 주민번호 보안 수집 → 위하고T 인적사항 등록
  - 4대보험 EDI 신고 (사무대행기관 지정 시)
- **퇴사자 처리**
  - 퇴직소득세 계산 → 위하고T 입력 → 신고 → 납부서
  - 4대보험 상실 신고
- **각종 지급명세서 자동화**
  - 근로소득 간이지급명세서 (매월)
  - 사업소득 간이지급명세서 (매월)

---

### Phase 4: 종합 세무 자동화 플랫폼 (18~24개월)

**확장 영역**
- 부가가치세 신고 보조
- 종합소득세·법인세 시즌 보조
- AI 기반 세무 컨설팅 어시스턴트 (거래처별 절세 제안)
- 다른 세무 프로그램 지원 (스마트A, 세무사랑 등)

---

## 3. Phase 1 MVP 상세 설계

### 3.1 핵심 데이터 모델 (개념도)

```
세무사사무소
   │
   └── 거래처 (Client)
         │
         ├── 직원 마스터 (Employee)
         │     ├── 이름, 주민번호(암호화), 입사일, 퇴사일
         │     └── 위하고T 직원ID (동기화용)
         │
         └── 월별 신고 (MonthlyFiling)
               │
               ├── 수집 세션 (CollectionSession)
               │     ├── 발송 알림톡 기록
               │     ├── 응답 수신 로그 (카톡/통화/엑셀)
               │     └── AI 파싱 결과
               │
               ├── 직원별 인건비 항목 (PayrollEntry)
               │     ├── 직원 매칭 결과 (MATCHED / NEW / RESIGNED / AMBIGUOUS)
               │     └── 검증 상태
               │
               └── 위하고T 표준 엑셀 출력 이력
```

### 3.2 AI 파싱 핵심 로직 (의사 코드)

```python
async def parse_payroll_message(
    raw_text: str,
    client_id: str,
    previous_month_data: dict,
    employee_master: list[Employee]
) -> PayrollParsingResult:
    """
    카톡/이메일 텍스트 → 구조화된 인건비 데이터 + 매칭 결과
    """
    prompt = f"""
    아래는 세무사 거래처가 보낸 이번 달 인건비 자료입니다.

    [거래처 직원 마스터]
    {employee_master}

    [이전 달 데이터]
    {previous_month_data}

    [이번 달 메시지]
    {raw_text}

    다음 JSON 형식으로 반환:
    {{
      "matched_employees": [
        {{"name": str, "employee_id": str, "amount": int,
          "change_from_prev": int, "change_reason": str | null}}
      ],
      "new_hire_suspected": [
        {{"name": str, "amount": int, "needs_confirmation": true}}
      ],
      "resignation_suspected": [
        {{"employee_id": str, "name": str, "reason": "이번달 누락"}}
      ],
      "ambiguous_items": [
        {{"raw_text": str, "issue": str}}
      ],
      "relative_references": [
        {{"text": "저번달과 동일", "applied": true}}
      ]
    }}
    """
    response = await claude_client.messages.create(
        model="claude-sonnet-4-5",
        messages=[{"role": "user", "content": prompt}]
    )
    return validate_and_parse(response)
```

### 3.3 위하고T 표준 엑셀 생성 (의사 코드)

위하고T의 [원천세 신고 → 일괄등록] 양식에 맞춘 컬럼 구성:

```python
# 위하고T 일괄등록용 표준 컬럼 (예시)
WEHAGO_COLUMNS = [
    "사번", "성명", "주민등록번호", "소득구분코드",
    "지급년월", "총지급액", "비과세소득", "과세소득",
    "소득세", "지방소득세", "원천징수일자"
]

def generate_wehago_excel(
    payroll_entries: list[PayrollEntry],
    period: str
) -> bytes:
    """
    AI가 정형화한 데이터를 위하고T 표준 엑셀로 변환
    - 소득세·지방소득세 자동 계산
    - 소득구분코드 자동 매핑 (근로/사업/기타 등)
    - 위하고T 양식 100% 호환
    """
    workbook = openpyxl.Workbook()
    ws = workbook.active
    ws.append(WEHAGO_COLUMNS)

    for entry in payroll_entries:
        tax = calculate_withholding_tax(entry)
        ws.append([
            entry.employee_code,
            entry.name,
            decrypt_rrn(entry.rrn_encrypted),
            map_income_type_code(entry.income_type),
            period,
            entry.total_amount,
            entry.non_taxable,
            entry.taxable,
            tax.income_tax,
            tax.local_tax,
            entry.payment_date
        ])

    return save_to_bytes(workbook)
```

### 3.4 후속 질문 자동 발송

신규 의심자가 감지되면 한 번에 묶어서 발송:

```
"확인이 필요한 사항이 있습니다 ✋

① 박민수님 — 신규 입사자이신가요?
   맞다면 아래 안전 입력 폼에서 주민번호·입사일을 입력해주세요:
   🔗 https://taxflow.ai/secure/abc123

② 김연호님 — 전월 100만원 → 이번달 200만원으로 증가했어요.
   상승 사유를 알려주세요. (보너스/급여인상/기타)

③ 이영수님 — 이번달 자료에서 누락되었어요. 퇴사하셨나요?"
```

---

## 4. 사업화·재무 계획

### 4.1 단계별 마일스톤

| 시기 | 목표 | 핵심 지표 |
|------|------|----------|
| 0~3개월 | Phase 1 MVP 개발 | 조명신 사무소 베타 적용 |
| 3~6개월 | 베타 고객 5~10곳 | AI 파싱 정확도 95%, NPS 50+ |
| 6~9개월 | 시드 투자 (3~5억) | Phase 2 개발 착수 |
| 9~18개월 | 유료 고객 100곳 | MRR 2,000만 원 |
| 18~24개월 | Phase 3·4 완성, Series A | 고객 500곳, MRR 1억 원 |

### 4.2 가격 정책 (가설)

| 플랜 | 월 요금 | 거래처 수 | 포함 단계 |
|------|---------|----------|----------|
| 스타터 | 15만 원 | 30곳 이하 | Phase 1 |
| 프로 | 25만 원 | 100곳 | Phase 1 + 2 |
| 엔터프라이즈 | 50만 원~ | 무제한 | 전 단계 |

### 4.3 GTM 전략

- **초기**: 조명신 대표 세무사 네트워크 → 인접 사무소 입소문
- **중기**: 한국세무사회 세미나, 세무 전문 유튜브 채널 협업
- **장기**: 더존·위하고 공식 파트너십 추진, 마켓플레이스 입점

---

## 5. 액션 아이템

### 즉시 (1~2주차)
- [ ] **위하고T 엑셀 일괄등록 양식 정밀 분석**
  - 실제 양식 다운로드 → 컬럼·코드체계 100% 파악
  - 소득구분코드, 세액 계산 로직 검증
- [ ] **조명신 사무소 실제 데이터 수집** (30~50건)
  - 카톡·통화·엑셀 샘플 → AI 파싱 정확도 벤치마크
- [ ] **개인정보보호 법무 자문**
  - 주민번호 수집·처리 절차 검토
  - Claude API 송신 데이터 마스킹 정책 검토
- [ ] **위하고T 자동화 정책 확인**
  - 약관에서 자동화 관련 조항 점검
- [ ] **NCP 계정 셋업 + KR-1/KR-2 리전 확보**
  - Cloud DB for PostgreSQL · Redis · Object Storage · KMS 활성화
  - NHN Cloud / 카카오비즈니스 알림톡 계정·템플릿 등록 시작 (심사 기간 고려)

### 3~8주차
- [ ] Claude API 파싱 프로토타입 (CLI 수준)
- [ ] CLOVA Speech 연동 PoC (통화 녹음 → 텍스트)
- [ ] Next.js 대시보드 골격 + 카카오 알림톡 발송 연동
- [ ] 거래처 응답 수신·파싱·검증 루프
- [ ] 위하고T 표준 엑셀 자동 생성기
- [ ] NKS 클러스터 + GitHub Actions → Container Registry 배포 파이프라인

### 9~12주차
- [ ] 조명신 사무소 실전 적용·피드백
- [ ] 외부 베타 세무사 1~2곳 모집
- [ ] ISMS-P / CSAP 인증 사전 준비 (보안 정책·로그·감사 체크리스트 작성)

---

## 6. 추후 결정·심화 검토 영역

기획서 v2.0 이후 더 깊이 파볼 후보:

1. **위하고T 일괄등록 양식 상세 분석** (실제 컬럼·코드 체계)
2. **Phase 1 프로토타입 코드 시작** (AI 파싱 + 표준 엑셀 생성)
3. **데스크톱 에이전트 아키텍처 설계** (Phase 2 대비)
4. **이 기획서를 Word/PDF로 정리해서 산출물로 받기**
