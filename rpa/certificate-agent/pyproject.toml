[project]
name = "certificate-agent"
version = "0.1.0"
description = "자동화 노트북 시뮬 (Phase 1.5) — 서버 잡 폴링·홈택스 발급·업로드. plan/17 §3-9 참조."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "keyring>=25",
    "typer>=0.12",
    "playwright>=1.47",
]
